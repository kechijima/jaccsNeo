rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // ログイン済み（モックログイン時は匿名認証セッション）ユーザーのみ読み書き可能
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
